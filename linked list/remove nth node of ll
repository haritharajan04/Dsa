class Solution{
  if(head != null ){
    System.out.println(head.data + "");
    head = head.next;
  }
    ListNodeDeleteNthNodeffromenf(Node head, int N){
      Node fastp = head;
      Node slowp = head;

      for(i=0; i<n; i++)
      fastp = fast.next;

      if(fastp == null)
        return head.next
    while(fastp != null){
        fastp = fast.next;
        slowp = slow.next;
  
  }
  Node delNode = slow.next;
  slow.next = slow.next.next;
  delNode = null;
  return head;
}
